from db import db

class EmployeeStudent():
    pass

    # __tablename__ = 'emp_stu'
    # emp_id = db.Column(db.Integer, db.ForeignKey('user.id'), primary_key=True)
    # stud_id = db.Column(db.Integer, db.ForeignKey('student.id'), primary_key=True)
    
    # def save_to_db(self):
    #     db.session.commit()
     


